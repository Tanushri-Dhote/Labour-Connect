# TODO

## Goal
Create API to fetch a labour profile for Postman testing.

## Steps
- [x] Implement `getLabourProfileService(userId)` in `src/modules/labour/labour.service.js`.
- [x] Add `getLabourProfile` controller method in `src/modules/labour/labour.controller.js`.
- [x] Add protected route `GET /api/labour/profile` in `src/modules/labour/labour.routes.js`.

- [x] (Optional) Add population of `categoryId` and `skills` in response.
- [ ] Test endpoint with Postman request details.
- [ ] Implement API to delete labour profile (and remove profile image file if present).



